
[build]
  publish = "dist/"
  functions = "scripts/netlify/api/"

[[redirects]]
  # SSR and SPA
  from = "/*"
  to = "/.netlify/functions/ssr"
  status = 200
  
  # SPA only
  # from = "/*"
  # to = "/__app.html"
  # status = 200